# 1. Install Python 3.13 with Homebrew
brew install python@3.13

# 2. Make sure your shell knows about it (optional but useful)
brew link python@3.13 --force --overwrite

# 3. Go into your YOLO project folder
cd /path/to/your/yolo-project

# 4. Create a virtual environment using Python 3.13
python3.13 -m venv .venv

# 5. Activate the virtual environment
source .venv/bin/activate

# 6. Abhängigkeiten installieren
pip install -r requirements.txt